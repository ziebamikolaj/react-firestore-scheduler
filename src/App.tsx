import { Paper } from "@mui/material";
import SchedulerComponent from "./components/SchedulerComponent";

const App = () => {
  return (
    <Paper>
      <SchedulerComponent />
    </Paper>
  );
};

export default App;
